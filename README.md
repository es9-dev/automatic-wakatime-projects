# Automatic Wakatime Projects Extension for VS Code

An extension that allows you to define rules for automating the population and configuration of `.wakatime-project` files across opened directories.
